# Local .terraform directories
**/.terraform/*

# .tfstate files (Contain sensitive info about your current infra)
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets.
*.tfvars
*.tfvars.json

# Ignore SSH keys if generated in this folder (Security Risk!)
*.pem
id_rsa*
*.pub

# Ignore override files as they are usually local customizations
override.tf
override.tf.json